# Throughput-Based CPU Calibration Configuration

# Default: 3 requests per second for sustained CPU calibration
TARGET_RPS=3

# Test duration - how long to maintain the throughput
TEST_DURATION=10m

# Base URL for the service
BASE_URL=http://localhost:8080

# Ramp down time for graceful shutdown
RAMP_DURATION=30s

# Alternative configurations for different scenarios:

# Light CPU calibration (1 RPS for 15 minutes)
# TARGET_RPS=1
# TEST_DURATION=15m

# Moderate CPU calibration (5 RPS for 8 minutes)  
# TARGET_RPS=5
# TEST_DURATION=8m

# Heavy CPU calibration (10 RPS for 5 minutes)
# TARGET_RPS=10
# TEST_DURATION=5m

# For Kubernetes testing, use the service URL:
# BASE_URL=http://resource-sizing-service.load-testing.svc.cluster.local:8080
